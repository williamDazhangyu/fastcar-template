application:
    env: "prod"
settings:
    koa:
        server:
            - { port: 80, host: "0.0.0.0" }
            # - { port: 443, host: "0.0.0.0", protocol: https, ssl: { key: "./ssl/server.key", cert: "./ssl/server.pem" } }
        koaStatic: #静态资源访问
            { "public": "public" } #前者为别名后者为相对于资源路径或者绝对路径
